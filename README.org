* BoTifiCator

Project for monitoring some selected bitcoin indicators

** Database

Up

#+begin_src bash
docker run --rm --name timescaledb-container \
  -e POSTGRES_PASSWORD=mysecretpassword \
  -v timescaledb_data:/var/lib/postgresql/data \
  -p 5432:5432 \
  -d timescale/timescaledb:latest-pg17
#+end_src

Interact

#+begin_src bash
psql -U postgres -p 5432 -h localhost
#+end_src

*** Moving Averages

**** Daily

#+headers: :engine postgresql :cmdline "postgres://postgres:mysecretpassword@localhost:5432/postgres"
#+begin_src sql
SELECT
        day,
        average,
        avg(average) over (ORDER BY day DESC ROWS BETWEEN CURRENT ROW AND 110 FOLLOWING) AS ma111,
        2 * avg(average) over (ORDER BY day DESC ROWS BETWEEN CURRENT ROW AND 349 FOLLOWING) AS ma350x2
FROM btc_daily_avg
ORDER BY day
LIMIT 3;
#+end_src

#+RESULTS:
| day                    |            average |              ma111 |             ma350x2 |
|------------------------+--------------------+--------------------+---------------------|
| 2026-03-08 00:00:00+00 | 67121.693069306931 | 82853.620357981744 | 197924.497690554770 |
| 2026-03-07 00:00:00+00 | 67809.795138888889 | 83096.297203453453 | 198024.594384920634 |
| 2026-03-06 00:00:00+00 | 69630.413194444444 | 83341.857482482482 | 198117.995376984126 |

**** Weekly

#+headers: :engine postgresql :cmdline "postgres://postgres:mysecretpassword@localhost:5432/postgres"
#+begin_src sql
SELECT
        day,
        average
FROM btc_weekly_avg
ORDER BY day DESC
LIMIT 3;
#+end_src

#+RESULTS:
|---|

**** Combined

#+headers: :engine postgresql :cmdline "postgres://postgres:mysecretpassword@localhost:5432/postgres"
#+begin_src sql
SELECT
        day,
        average
FROM btc_daily_avg btc_weekly_avg,
ORDER BY day DESC
LIMIT 3;
#+end_src

** Data

bitstamp.com http api

*** Rate Limits

400 per second

#+begin_src julia
1000 / 400
#+end_src

#+RESULTS:
: 2.5

10 000 per 10 minutes

#+begin_src julia
1000 * 60 * 10 / 10000
#+end_src

#+RESULTS:
: 60.0

=> wait 100 ms per request just to be safe
